# ── Server ────────────────────────────────────────────────────────────────────
HOST=0.0.0.0
PORT=8000

# ── Risk thresholds (0–100) ───────────────────────────────────────────────────
# Transactions below SCORE_APPROVE are auto-approved
SCORE_APPROVE=40
# Transactions above SCORE_BLOCK are auto-blocked; between = manual review
SCORE_BLOCK=70

# ── Geolocation ───────────────────────────────────────────────────────────────
# Free tier: 45 req/min. Replace with MaxMind for production.
# GEOIP_PROVIDER=ip-api.com

# ── BIN Lookup (HandyAPI) ─────────────────────────────────────────────────────
# Get your free key at https://www.handyapi.com/dashboard
# Used to detect card issuer bank via the first 6-8 digits (BIN/IIN)
HANDY_API_KEY=your_handyapi_key_here

# ── Data paths ────────────────────────────────────────────────────────────────
# HISTORY_FILE=data/transactions.json
# BLOCKED_FILE=data/blocked.json
# MODELS_DIR=models/
